//#region source/params/helpers.d.ts
/**
 * Checks if the given runtime action input value is non-empty.
 *
 * @param name - The name of the parameter. Required because of `aio app dev` compatibility: inputs mapped to undefined env vars come as $<input_name> in dev mode, but as '' in prod mode.
 * @param value - The value of the parameter.
 */
declare function nonEmpty(name: string, value: unknown): boolean;
/**
 * Checks if all required parameters are non-empty.
 *
 * @param params - The action input parameters.
 * @param required - The list of required parameter names.
 */
declare function allNonEmpty<const T extends string[]>(params: Record<string, unknown>, required: T): params is Record<string, unknown> & Record<T[number], unknown>;
//#endregion
//#region source/params/types.d.ts
/**
 * Standard HTTP methods supported by Adobe I/O Runtime.
 * These are the methods that can be used when invoking runtime actions via HTTP.
 */
type HttpMethod = "GET" | "POST" | "PUT" | "PATCH" | "DELETE" | "HEAD" | "OPTIONS";
/**
 * Lowercase HTTP method as sent by OpenWhisk/Adobe I/O Runtime.
 * OpenWhisk sends methods in lowercase (e.g., "get", "post").
 */
type HttpMethodLowercase = Lowercase<HttpMethod>;
/** The type of the runtime action parameters. */
type RuntimeActionParams = {
  /** If the runtime action is invoked via HTTP, this will be the headers of the request. */__ow_headers?: Record<string, string | undefined>; /** If the runtime action is invoked via HTTP, this will be the HTTP method of the request. */
  __ow_method?: HttpMethodLowercase; /** If the runtime action is invoked via HTTP, this will be the unmatched path of the request (matching stops after consuming the action extension) */
  __ow_path?: string; /** If the runtime action is invoked via HTTP, this will be the request body entity, as a base64-encoded string when its content is binary or JSON object/array, or as a plain string otherwise */
  __ow_body?: string; /** If the runtime action is invoked via HTTP, this will be the query parameters of the request, as an unparsed string. */
  __ow_query?: string;
  [key: string]: unknown;
};
//#endregion
export { nonEmpty as a, allNonEmpty as i, HttpMethodLowercase as n, RuntimeActionParams as r, HttpMethod as t };