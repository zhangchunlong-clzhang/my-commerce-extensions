/**
 * @license
 *
 * Copyright 2026 Adobe. All rights reserved.
 * This file is licensed to you under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License. You may obtain a copy
 * of the License at http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software distributed under
 * the License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR REPRESENTATIONS
 * OF ANY KIND, either express or implied. See the License for the specific language
 * governing permissions and limitations under the License.
 */

import * as v from "valibot";
import { CommerceHttpClientParams } from "@adobe/aio-commerce-lib-api";
//#region source/modules/schema/fields.d.ts
/** Schema for a configuration field that can be one of various field types (list, text, password, email, url, or phone) */
declare const FieldSchema: v.VariantSchema<"type", [v.VariantSchema<"selectionMode", [v.ObjectSchema<{
  readonly type: v.LiteralSchema<"list", "Expected the type to be 'list'">;
  readonly selectionMode: v.LiteralSchema<"single", "Expected the selectionMode to be 'single'">;
  readonly options: v.ArraySchema<v.ObjectSchema<{
    readonly label: v.StringSchema<"Expected a string for the option label">;
    readonly value: v.StringSchema<"Expected a string for the option value">;
  }, undefined>, "Expected an array of list options">;
  readonly default: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default value">, v.NonEmptyAction<string, "The default value must not be empty">]>;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"list", "Expected the type to be 'list'">;
  readonly selectionMode: v.LiteralSchema<"multiple", "Expected the selectionMode to be 'multiple'">;
  readonly options: v.ArraySchema<v.ObjectSchema<{
    readonly label: v.StringSchema<"Expected a string for the option label">;
    readonly value: v.StringSchema<"Expected a string for the option value">;
  }, undefined>, "Expected an array of list options">;
  readonly default: v.OptionalSchema<v.ArraySchema<v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for each default value">, v.NonEmptyAction<string, "Each default value must not be empty">]>, "Expected an array of default values">, readonly []>;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>], undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"text", "Expected the type to be 'text'">;
  readonly default: v.OptionalSchema<v.StringSchema<"Expected a string for the default value">, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"password", "Expected the type to be 'password'">;
  readonly default: v.OptionalSchema<v.LiteralSchema<"", "Password fields do not have a default value">, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"email", "Expected the type to be 'email'">;
  readonly default: v.OptionalSchema<v.UnionSchema<[v.LiteralSchema<"", undefined>, v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default email value">, v.EmailAction<string, "The email must be a valid email address">]>], undefined>, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"url", "Expected the type to be 'url'">;
  readonly default: v.OptionalSchema<v.UnionSchema<[v.LiteralSchema<"", undefined>, v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default URL value">, v.UrlAction<string, "The URL must be a valid URL">]>], undefined>, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"tel", "Expected the type to be 'tel'">;
  readonly default: v.OptionalSchema<v.UnionSchema<[v.LiteralSchema<"", undefined>, v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default phone number value">, v.RegexAction<string, "The phone number must contain only numbers and/or country codes">]>], undefined>, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"boolean", "Expected the type to be 'boolean'">;
  readonly default: v.OptionalSchema<v.BooleanSchema<"Expected a boolean for the default value">, false>;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>], undefined>;
/** Schema for the schema of the business configuration, which is an array of configuration fields with at least one field required */
declare const SchemaBusinessConfigSchema: v.SchemaWithPipe<readonly [v.ArraySchema<v.VariantSchema<"type", [v.VariantSchema<"selectionMode", [v.ObjectSchema<{
  readonly type: v.LiteralSchema<"list", "Expected the type to be 'list'">;
  readonly selectionMode: v.LiteralSchema<"single", "Expected the selectionMode to be 'single'">;
  readonly options: v.ArraySchema<v.ObjectSchema<{
    readonly label: v.StringSchema<"Expected a string for the option label">;
    readonly value: v.StringSchema<"Expected a string for the option value">;
  }, undefined>, "Expected an array of list options">;
  readonly default: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default value">, v.NonEmptyAction<string, "The default value must not be empty">]>;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"list", "Expected the type to be 'list'">;
  readonly selectionMode: v.LiteralSchema<"multiple", "Expected the selectionMode to be 'multiple'">;
  readonly options: v.ArraySchema<v.ObjectSchema<{
    readonly label: v.StringSchema<"Expected a string for the option label">;
    readonly value: v.StringSchema<"Expected a string for the option value">;
  }, undefined>, "Expected an array of list options">;
  readonly default: v.OptionalSchema<v.ArraySchema<v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for each default value">, v.NonEmptyAction<string, "Each default value must not be empty">]>, "Expected an array of default values">, readonly []>;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>], undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"text", "Expected the type to be 'text'">;
  readonly default: v.OptionalSchema<v.StringSchema<"Expected a string for the default value">, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"password", "Expected the type to be 'password'">;
  readonly default: v.OptionalSchema<v.LiteralSchema<"", "Password fields do not have a default value">, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"email", "Expected the type to be 'email'">;
  readonly default: v.OptionalSchema<v.UnionSchema<[v.LiteralSchema<"", undefined>, v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default email value">, v.EmailAction<string, "The email must be a valid email address">]>], undefined>, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"url", "Expected the type to be 'url'">;
  readonly default: v.OptionalSchema<v.UnionSchema<[v.LiteralSchema<"", undefined>, v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default URL value">, v.UrlAction<string, "The URL must be a valid URL">]>], undefined>, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"tel", "Expected the type to be 'tel'">;
  readonly default: v.OptionalSchema<v.UnionSchema<[v.LiteralSchema<"", undefined>, v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default phone number value">, v.RegexAction<string, "The phone number must contain only numbers and/or country codes">]>], undefined>, "">;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>, v.ObjectSchema<{
  readonly type: v.LiteralSchema<"boolean", "Expected the type to be 'boolean'">;
  readonly default: v.OptionalSchema<v.BooleanSchema<"Expected a boolean for the default value">, false>;
  readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
  readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
  readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
  readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
}, undefined>], undefined>, "Expected an array of configuration fields">, v.MinLengthAction<({
  type: "list";
  selectionMode: "single";
  options: {
    label: string;
    value: string;
  }[];
  default: string;
  name: string;
  label?: string | undefined;
  description?: string | undefined;
  env?: ("paas" | "saas")[] | undefined;
} | {
  type: "list";
  selectionMode: "multiple";
  options: {
    label: string;
    value: string;
  }[];
  default: string[];
  name: string;
  label?: string | undefined;
  description?: string | undefined;
  env?: ("paas" | "saas")[] | undefined;
} | {
  type: "text";
  default: string;
  name: string;
  label?: string | undefined;
  description?: string | undefined;
  env?: ("paas" | "saas")[] | undefined;
} | {
  type: "password";
  default: "";
  name: string;
  label?: string | undefined;
  description?: string | undefined;
  env?: ("paas" | "saas")[] | undefined;
} | {
  type: "email";
  default: string;
  name: string;
  label?: string | undefined;
  description?: string | undefined;
  env?: ("paas" | "saas")[] | undefined;
} | {
  type: "url";
  default: string;
  name: string;
  label?: string | undefined;
  description?: string | undefined;
  env?: ("paas" | "saas")[] | undefined;
} | {
  type: "tel";
  default: string;
  name: string;
  label?: string | undefined;
  description?: string | undefined;
  env?: ("paas" | "saas")[] | undefined;
} | {
  type: "boolean";
  default: boolean;
  name: string;
  label?: string | undefined;
  description?: string | undefined;
  env?: ("paas" | "saas")[] | undefined;
})[], 1, "At least one configuration parameter is required">]>;
//#endregion
//#region source/modules/schema/types.d.ts
/**
 * The schema type for a configuration field.
 *
 * Represents a single field definition in the configuration schema, which can be
 * one of various types: list, text, password, email, url, phone, or boolean.
 */
type BusinessConfigSchemaField = v.InferInput<typeof FieldSchema>;
/**
 * The schema type for the business configuration schema.
 *
 * Represents an array of configuration field definitions that make up the complete
 * business configuration schema. Must contain at least one field.
 */
type BusinessConfigSchema = v.InferInput<typeof SchemaBusinessConfigSchema>;
/** The schema type for the business configuration schema. */
type BusinessConfigSchemaValue = BusinessConfigSchemaField["default"];
/**
 * The schema type for an option in a list configuration field.
 * Represents a single option that can be selected in a list-type configuration field.
 */
type BusinessConfigSchemaListOption = Extract<BusinessConfigSchemaField, {
  type: "list";
}>["options"][number];
//#endregion
//#region source/modules/configuration/types.d.ts
/**
 * Represents the origin of a configuration value, indicating which scope it came from.
 */
type ConfigOrigin = {
  /** The scope code where the configuration value originates. */code: string; /** The scope level where the configuration value originates. */
  level: string;
};
/**
 * Represents a configuration value with its origin information.
 */
type ConfigValue = {
  /** The name of the configuration field. */name: string; /** The configuration value (string, number, boolean, or undefined). */
  value: BusinessConfigSchemaValue; /** The origin scope where this value was set or inherited from. */
  origin: ConfigOrigin;
};
/**
 * A configuration entry used in set/patch requests.
 *
 * Value can be `null` to unset the field, which removes the explicit override
 * at the current scope and restores inheritance from the parent scope.
 *
 * @internal
 */
type SetConfigValue = {
  /** The name of the configuration field. */name: string;
  /**
   * The value to set (string, number, or boolean).
   * Pass `null` to unset the field, removing the explicit override at the
   * current scope and restoring inheritance from the parent scope.
   */
  value: BusinessConfigSchemaValue | null;
};
//#endregion
//#region source/modules/scope-tree/types.d.ts
/**
 * Represents a single node in the scope tree hierarchy.
 */
type ScopeNode = {
  /** Unique identifier for the scope. */id: string; /** Unique code identifier for the scope. */
  code: string; /** Human-readable label for the scope. */
  label: string; /** The level of the scope (e.g., "global", "website", "store", "store_view"). */
  level: string; /** Whether the scope configuration can be edited. */
  is_editable: boolean; /** Whether this is a final (leaf) scope that cannot have children. */
  is_final: boolean; /** Whether the scope can be removed. */
  is_removable: boolean; /** Optional Commerce API ID for system scopes. */
  commerce_id?: number; /** Optional child scopes for hierarchical structures. */
  children?: ScopeNode[];
};
/**
 * Represents the complete scope tree as an array of root scope nodes.
 */
type ScopeTree = ScopeNode[];
/**
 * Result from getting the scope tree.
 */
type GetScopeTreeResult = {
  /** The scope tree as an array of root scope nodes. */scopeTree: ScopeNode[]; /** Whether the returned data came from cache. */
  isCachedData: boolean; /** Optional error message if fallback data was used. */
  fallbackError?: string;
};
//#endregion
//#region source/types/api.d.ts
/**
 * Response type for getting the configuration schema.
 */
type GetConfigSchemaResponse = {
  /** Array of configuration schema field definitions. */configSchema: BusinessConfigSchema;
};
/**
 * Response type for getting configuration for a scope.
 */
type GetConfigurationResponse = {
  /** Scope information including id, code, and level. */scope: {
    id: string;
    code: string;
    level: string;
  }; /** Array of configuration values with their origins. */
  config: ConfigValue[];
};
/**
 * Response type for getting a single configuration value by key.
 */
type GetConfigurationByKeyResponse = {
  /** Scope information including id, code, and level. */scope: {
    id: string;
    code: string;
    level: string;
  }; /** The configuration value, or null if not found. */
  config: ConfigValue | null;
};
/**
 * Request type for setting configuration values.
 */
type SetConfigurationRequest = {
  /** Array of configuration name-value pairs to set. */config: SetConfigValue[];
};
/**
 * Response type for setting configuration values.
 */
type SetConfigurationResponse = {
  /** Success message. */message: string; /** ISO timestamp of when the configuration was updated. */
  timestamp: string; /** Scope information including id, code, and level. */
  scope: {
    id: string;
    code: string;
    level: string;
  }; /** Array of updated configuration values. */
  config: Array<{
    name: string;
    value: BusinessConfigSchemaValue;
  }>;
};
/**
 * Request type for setting custom scope tree.
 */
type SetCustomScopeTreeRequest = {
  /** Array of custom scope definitions to set. */scopes: CustomScopeInput[];
};
/**
 * Input type for a custom scope definition.
 */
type CustomScopeInput = {
  /** Optional scope ID. If not provided, a new scope will be created. */id?: string; /** Unique code identifier for the scope. */
  code: string; /** Human-readable label for the scope. */
  label: string; /** Optional level. Defaults to base level if not provided. */
  level?: string; /** Whether the scope configuration can be edited. */
  is_editable: boolean; /** Whether this is a final (leaf) scope that cannot have children. */
  is_final: boolean; /** Optional child scopes for hierarchical structures. */
  children?: CustomScopeInput[];
};
/**
 * Response type for setting custom scope tree.
 */
type SetCustomScopeTreeResponse = {
  /** Success message. */message: string; /** ISO timestamp of when the custom scope tree was updated. */
  timestamp: string; /** Array of created/updated custom scopes with assigned IDs. */
  scopes: CustomScopeOutput[];
};
/**
 * Output type for a custom scope definition (includes assigned ID).
 */
type CustomScopeOutput = {
  /** Assigned scope ID. */id: string; /** Unique code identifier for the scope. */
  code: string; /** Human-readable label for the scope. */
  label: string; /** Scope level. */
  level: string; /** Whether the scope configuration can be edited. */
  is_editable: boolean; /** Whether this is a final (leaf) scope that cannot have children. */
  is_final: boolean; /** Optional child scopes for hierarchical structures. */
  children?: CustomScopeOutput[];
};
//#endregion
//#region source/types/commerce.d.ts
/**
 * Commerce API response data containing websites, store groups, and store views.
 *
 * This type represents the structure of scope data returned from the Adobe Commerce API
 * when fetching scope information.
 */
type CommerceScopeData = {
  /** Array of website definitions. */websites: Website[]; /** Array of store group definitions. */
  storeGroups: StoreGroup[]; /** Array of store view definitions. */
  storeViews: StoreView[];
};
/**
 * Represents a website in Adobe Commerce.
 */
type Website = {
  /** Unique identifier for the website. */id: number; /** Display name of the website. */
  name: string; /** Unique code identifier for the website. */
  code: string; /** ID of the default store group for this website. */
  default_group_id: number; /** Optional extension attributes for additional data. */
  extension_attributes?: Record<string, unknown>;
};
/**
 * Represents a store group in Adobe Commerce.
 */
type StoreGroup = {
  /** Unique identifier for the store group. */id: number; /** ID of the parent website. */
  website_id: number; /** Root category ID for this store group. */
  root_category_id: number; /** ID of the default store for this store group. */
  default_store_id: number; /** Display name of the store group. */
  name: string; /** Unique code identifier for the store group. */
  code: string; /** Optional extension attributes for additional data. */
  extension_attributes?: Record<string, unknown>;
};
/**
 * Represents a store view in Adobe Commerce.
 */
type StoreView = {
  /** Unique identifier for the store view. */id: number; /** Unique code identifier for the store view. */
  code: string; /** Display name of the store view. */
  name: string; /** ID of the parent website. */
  website_id: number; /** ID of the parent store group. */
  store_group_id: number; /** Whether the store view is currently active. */
  is_active: boolean; /** Optional extension attributes for additional data. */
  extension_attributes?: Record<string, unknown>;
};
//#endregion
//#region source/types/index.d.ts
/** Options for controlling operations of the configuration library. */
type OperationOptions = {
  /** Optional cache timeout in milliseconds. */cacheTimeout?: number;
};
/** Options for controlling configuration operations. */
type ConfigOptions = OperationOptions & {
  /** Optional encryption key for encrypting/decrypting password fields. */encryptionKey?: string;
};
//#endregion
//#region source/modules/schema/index.d.ts
/** The schema used to validate the the business configuration settings. */
declare const SchemaBusinessConfig: v.ObjectSchema<{
  readonly schema: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.VariantSchema<"type", [v.VariantSchema<"selectionMode", [v.ObjectSchema<{
    readonly type: v.LiteralSchema<"list", "Expected the type to be 'list'">;
    readonly selectionMode: v.LiteralSchema<"single", "Expected the selectionMode to be 'single'">;
    readonly options: v.ArraySchema<v.ObjectSchema<{
      readonly label: v.StringSchema<"Expected a string for the option label">;
      readonly value: v.StringSchema<"Expected a string for the option value">;
    }, undefined>, "Expected an array of list options">;
    readonly default: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default value">, v.NonEmptyAction<string, "The default value must not be empty">]>;
    readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
    readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
    readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
    readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
  }, undefined>, v.ObjectSchema<{
    readonly type: v.LiteralSchema<"list", "Expected the type to be 'list'">;
    readonly selectionMode: v.LiteralSchema<"multiple", "Expected the selectionMode to be 'multiple'">;
    readonly options: v.ArraySchema<v.ObjectSchema<{
      readonly label: v.StringSchema<"Expected a string for the option label">;
      readonly value: v.StringSchema<"Expected a string for the option value">;
    }, undefined>, "Expected an array of list options">;
    readonly default: v.OptionalSchema<v.ArraySchema<v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for each default value">, v.NonEmptyAction<string, "Each default value must not be empty">]>, "Expected an array of default values">, readonly []>;
    readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
    readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
    readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
    readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
  }, undefined>], undefined>, v.ObjectSchema<{
    readonly type: v.LiteralSchema<"text", "Expected the type to be 'text'">;
    readonly default: v.OptionalSchema<v.StringSchema<"Expected a string for the default value">, "">;
    readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
    readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
    readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
    readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
  }, undefined>, v.ObjectSchema<{
    readonly type: v.LiteralSchema<"password", "Expected the type to be 'password'">;
    readonly default: v.OptionalSchema<v.LiteralSchema<"", "Password fields do not have a default value">, "">;
    readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
    readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
    readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
    readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
  }, undefined>, v.ObjectSchema<{
    readonly type: v.LiteralSchema<"email", "Expected the type to be 'email'">;
    readonly default: v.OptionalSchema<v.UnionSchema<[v.LiteralSchema<"", undefined>, v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default email value">, v.EmailAction<string, "The email must be a valid email address">]>], undefined>, "">;
    readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
    readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
    readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
    readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
  }, undefined>, v.ObjectSchema<{
    readonly type: v.LiteralSchema<"url", "Expected the type to be 'url'">;
    readonly default: v.OptionalSchema<v.UnionSchema<[v.LiteralSchema<"", undefined>, v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default URL value">, v.UrlAction<string, "The URL must be a valid URL">]>], undefined>, "">;
    readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
    readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
    readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
    readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
  }, undefined>, v.ObjectSchema<{
    readonly type: v.LiteralSchema<"tel", "Expected the type to be 'tel'">;
    readonly default: v.OptionalSchema<v.UnionSchema<[v.LiteralSchema<"", undefined>, v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the default phone number value">, v.RegexAction<string, "The phone number must contain only numbers and/or country codes">]>], undefined>, "">;
    readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
    readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
    readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
    readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
  }, undefined>, v.ObjectSchema<{
    readonly type: v.LiteralSchema<"boolean", "Expected the type to be 'boolean'">;
    readonly default: v.OptionalSchema<v.BooleanSchema<"Expected a boolean for the default value">, false>;
    readonly name: v.SchemaWithPipe<readonly [v.StringSchema<"Expected a string for the field name">, v.NonEmptyAction<string, "The field name must not be empty">]>;
    readonly label: v.OptionalSchema<v.StringSchema<"Expected a string for the field label">, undefined>;
    readonly description: v.OptionalSchema<v.StringSchema<"Expected a string for the field description">, undefined>;
    readonly env: v.OptionalSchema<v.SchemaWithPipe<readonly [v.ArraySchema<v.PicklistSchema<readonly ["paas", "saas"], `Expected one of: ${string}`>, "Expected an array of commerce environments for the field \"env\"">, v.NonEmptyAction<("paas" | "saas")[], "The \"env\" array must contain at least one commerce environment">]>, undefined>;
  }, undefined>], undefined>, "Expected an array of configuration fields">, v.MinLengthAction<({
    type: "list";
    selectionMode: "single";
    options: {
      label: string;
      value: string;
    }[];
    default: string;
    name: string;
    label?: string | undefined;
    description?: string | undefined;
    env?: ("paas" | "saas")[] | undefined;
  } | {
    type: "list";
    selectionMode: "multiple";
    options: {
      label: string;
      value: string;
    }[];
    default: string[];
    name: string;
    label?: string | undefined;
    description?: string | undefined;
    env?: ("paas" | "saas")[] | undefined;
  } | {
    type: "text";
    default: string;
    name: string;
    label?: string | undefined;
    description?: string | undefined;
    env?: ("paas" | "saas")[] | undefined;
  } | {
    type: "password";
    default: "";
    name: string;
    label?: string | undefined;
    description?: string | undefined;
    env?: ("paas" | "saas")[] | undefined;
  } | {
    type: "email";
    default: string;
    name: string;
    label?: string | undefined;
    description?: string | undefined;
    env?: ("paas" | "saas")[] | undefined;
  } | {
    type: "url";
    default: string;
    name: string;
    label?: string | undefined;
    description?: string | undefined;
    env?: ("paas" | "saas")[] | undefined;
  } | {
    type: "tel";
    default: string;
    name: string;
    label?: string | undefined;
    description?: string | undefined;
    env?: ("paas" | "saas")[] | undefined;
  } | {
    type: "boolean";
    default: boolean;
    name: string;
    label?: string | undefined;
    description?: string | undefined;
    env?: ("paas" | "saas")[] | undefined;
  })[], 1, "At least one configuration parameter is required">]>, readonly []>;
}, undefined>;
/** Defines the shape of the business configuration settings. */
type BusinessConfig = v.InferInput<typeof SchemaBusinessConfig>;
//#endregion
//#region source/config-utils.d.ts
/**
 * Selector type for identifying a scope by its unique ID.
 */
type SelectorByScopeId = {
  by: {
    _tag: "scopeId";
    scopeId: string;
  };
};
/**
 * Selector type for identifying a scope by its code and level.
 */
type SelectorByCodeAndLevel = {
  by: {
    _tag: "codeAndLevel";
    code: string;
    level: string;
  };
};
/**
 * Selector type for identifying a scope by its code only.
 */
type SelectorByCode = {
  by: {
    _tag: "code";
    code: string;
  };
};
/**
 * Discriminated union type for selecting a scope by different methods.
 *
 * Use the helper functions {@link byScopeId}, {@link byCodeAndLevel}, or {@link byCode} to create
 * selector objects instead of constructing them manually.
 */
type SelectorBy = SelectorByScopeId | SelectorByCodeAndLevel | SelectorByCode;
/**
 * Creates a scope selector that identifies a scope by its unique ID.
 *
 * This is the most direct way to identify a scope when you already know its ID.
 * Scope IDs are unique identifiers assigned to each scope in the scope tree.
 *
 * @param scopeId - The unique identifier of the scope.
 * @returns A selector object that identifies the scope by ID.
 *
 * @example
 * ```typescript
 * import { getConfiguration, byScopeId } from "@adobe/aio-commerce-lib-config";
 *
 * // Get configuration for a scope by its ID
 * const config = await getConfiguration(byScopeId("scope-123"));
 * ```
 */
declare function byScopeId(scopeId: string): SelectorByScopeId;
/**
 * Creates a scope selector that identifies a scope by its code and level.
 *
 * This selector is useful when you know both the scope code and its level in
 * the hierarchy (e.g., "website" level "website", "store" level "store").
 *
 * @param code - The code identifier of the scope (e.g., "us-east", "main_store").
 * @param level - The level of the scope in the hierarchy (e.g., "website", "store", "store_view").
 * @returns A selector object that identifies the scope by code and level.
 *
 * @example
 * ```typescript
 * import { getConfiguration, byCodeAndLevel } from "@adobe/aio-commerce-lib-config";
 *
 * // Get configuration for a website scope
 * const config = await getConfiguration(byCodeAndLevel("us-east", "website"));
 *
 * // Get configuration for a store scope
 * const storeConfig = await getConfiguration(byCodeAndLevel("main_store", "store"));
 * ```
 */
declare function byCodeAndLevel(code: string, level: string): SelectorByCodeAndLevel;
/**
 * Creates a scope selector that identifies a scope by its code only.
 *
 * This selector uses the scope code and will resolve to the default level for that code.
 * Use this when you want to let the system determine the appropriate level based on
 * the code alone.
 *
 * @param code - The code identifier of the scope.
 * @returns A selector object that identifies the scope by code.
 *
 * @example
 * ```typescript
 * import { getConfiguration, byCode } from "@adobe/aio-commerce-lib-config";
 *
 * // Get configuration by code (uses default level)
 * const config = await getConfiguration(byCode("us-east"));
 * ```
 */
declare function byCode(code: string): SelectorByCode;
//#endregion
//#region source/utils/repository.d.ts
/** Defines the options for initializing the Adobe State library. */
type LibStateOptions = {
  /**
   * The region where `aio-lib-state` should operate. By default, the most optimal region is
   * automatically determined based on where the runtime action is running.
   *
   * @see https://developer.adobe.com/app-builder/docs/guides/app_builder_guides/storage/application-state#state
   */
  region?: "amer" | "emea" | "apac" | "aus";
};
//#endregion
//#region source/config-manager.d.ts
/** Options for initializing the configuration library, so that it works as expected. */
type InitializeOptions = {
  /** Optional schema to use as the source of truth (latest version). If not provided, it will use the stored one (but only if it exists). */schema?: BusinessConfigSchema; /** The options for initializing the Adobe State library (used for caching). */
  libStateOptions?: LibStateOptions;
};
/**
 * Initializes the configuration library so that it works as expected.
 * The schema is stored in global memory. If a schema is provided, it will be set.
 * If no schema is provided, initialization will succeed only if a schema was previously set globally.
 * @param options - Options for initializing the configuration library.
 */
declare function initialize(options: InitializeOptions): void;
/** Parameters for getting the scope tree from Commerce API. */
type GetFreshScopeTreeParams = {
  refreshData: true;
  commerceConfig: CommerceHttpClientParams;
};
/** Parameters for getting the scope tree from cache. */
type GetCachedScopeTreeParams = {
  refreshData?: false | undefined;
};
/**
 * Gets the scope tree from cache or Commerce API.
 *
 * The scope tree represents the hierarchical structure of configuration scopes available
 * in your Adobe Commerce instance. This includes both system scopes (global, website, store)
 * and custom scopes that may have been defined.
 *
 * @param params - Configuration options. If `refreshData` is true, `commerceConfig` is required.
 * @param options - Optional library configuration options for cache timeout.
 * @returns Promise resolving to scope tree with metadata about data freshness and any fallback information.
 *
 * @example
 * ```typescript
 * import { getScopeTree } from "@adobe/aio-commerce-lib-config";
 *
 * // Get cached scope tree (default behavior)
 * const result = await getScopeTree();
 * console.log(result.scopeTree); // Array of scope nodes
 * console.log(result.isCachedData); // true
 * ```
 *
 * @example
 * ```typescript
 * import { getScopeTree } from "@adobe/aio-commerce-lib-config";
 * import type { CommerceHttpClientParams } from "@adobe/aio-commerce-lib-api";
 *
 * // Refresh scope tree from Commerce API
 * const commerceConfig: CommerceHttpClientParams = {
 *   url: "https://your-commerce-instance.com",
 *   // ... other auth config
 * };
 *
 * const result = await getScopeTree(
 *   { refreshData: true, commerceConfig },
 *   { cacheTimeout: 600000 }
 * );
 * console.log(result.scopeTree); // Fresh data from Commerce API
 * console.log(result.isCachedData); // false
 * if (result.fallbackError) {
 *   console.warn("Used fallback data:", result.fallbackError);
 * }
 * ```
 *
 * @example
 * ```typescript
 * import { getScopeTree } from "@adobe/aio-commerce-lib-config";
 *
 * // Get scope tree with custom cache timeout
 * const result = await getScopeTree(undefined, { cacheTimeout: 600000 });
 * ```
 */
declare function getScopeTree(params?: GetCachedScopeTreeParams, options?: OperationOptions): Promise<GetScopeTreeResult>;
declare function getScopeTree(params: GetFreshScopeTreeParams, options?: OperationOptions): Promise<GetScopeTreeResult>;
/**
 * Syncs Commerce scopes by forcing a fresh fetch from Commerce API and updating the cache.
 *
 * This function is useful when you need to ensure your scope tree is up-to-date with
 * the latest changes from your Commerce instance. It will fetch fresh data and update
 * both the cache and persistent storage.
 *
 * @param commerceConfig - The Commerce HTTP client configuration required for API calls.
 * @param options - Optional library configuration options for cache timeout.
 * @returns Promise resolving to sync result with updated scope tree and sync status.
 *
 * @example
 * ```typescript
 * import { syncCommerceScopes } from "@adobe/aio-commerce-lib-config";
 * import type { CommerceHttpClientParams } from "@adobe/aio-commerce-lib-api";
 *
 * const commerceConfig: CommerceHttpClientParams = {
 *   url: "https://your-commerce-instance.com",
 *   // ... other auth config
 * };
 *
 * const result = await syncCommerceScopes(commerceConfig);
 *
 * if (result.synced) {
 *   console.log("Successfully synced scope tree");
 *   console.log(result.scopeTree); // Updated scope tree
 * } else {
 *   console.log("Used cached data");
 * }
 *
 * if (result.error) {
 *   console.warn("Sync completed with errors:", result.error);
 * }
 * ```
 */
declare function syncCommerceScopes(commerceConfig: CommerceHttpClientParams, options?: OperationOptions): Promise<{
  scopeTree: ScopeTree;
  synced: boolean;
  error?: string;
}>;
/**
 * Removes the commerce scope from the persisted scope tree.
 *
 * @returns Promise resolving to an object with `unsynced` boolean indicating whether the
 * scope was found and removed, or `false` if it was already not present.
 *
 * @example
 * ```typescript
 * import { unsyncCommerceScopes } from "@adobe/aio-commerce-lib-config";
 *
 * const result = await unsyncCommerceScopes();
 * if (result.unsynced) {
 *   console.log("Commerce scope removed successfully");
 * } else {
 *   console.log("Commerce scope not found");
 * }
 * ```
 */
declare function unsyncCommerceScopes(): Promise<{
  unsynced: boolean;
}>;
/**
 * Gets configuration for a scope.
 *
 * This function retrieves all configuration values for a specific scope, including
 * inherited values from parent scopes and schema defaults. The configuration is
 * merged according to the scope hierarchy.
 *
 * @param selector - Scope selector specifying how to identify the scope.
 * @param options - Optional library configuration options for cache timeout.
 * @returns Promise resolving to configuration response with scope information and config values.
 *
 * @example
 * ```typescript
 * import { getConfiguration, byScopeId, byCodeAndLevel, byCode } from "@adobe/aio-commerce-lib-config";
 *
 * // Get configuration by scope ID
 * const config1 = await getConfiguration(byScopeId("scope-123"));
 * console.log(config1.scope); // { id, code, level }
 * console.log(config1.config); // Array of config values with origins
 *
 * // Get configuration by code and level
 * const config2 = await getConfiguration(byCodeAndLevel("website", "website"));
 * config2.config.forEach((item) => {
 *   console.log(`${item.name}: ${item.value} (from ${item.origin.code})`);
 * });
 *
 * // Get configuration by code (uses default level)
 * const config3 = await getConfiguration(byCode("website"));
 * ```
 */
declare function getConfiguration(selector: SelectorBy, options?: ConfigOptions): Promise<GetConfigurationResponse>;
/**
 * Gets a specific configuration value by key for a scope.
 *
 * This function retrieves a single configuration value for a specific scope. It's useful
 * when you only need one configuration field rather than the entire configuration object.
 *
 * @param configKey - The name of the configuration field to retrieve.
 * @param selector - Scope selector specifying how to identify the scope.
 * @param options - Optional library configuration options for cache timeout.
 * @returns Promise resolving to configuration response with scope information and single config value (or null if not found).
 *
 * @example
 * ```typescript
 * import { getConfigurationByKey, byScopeId, byCodeAndLevel, byCode } from "@adobe/aio-commerce-lib-config";
 *
 * // Get a specific config value by scope ID
 * const result1 = await getConfigurationByKey("api_key", byScopeId("scope-123"));
 *
 * if (result1.config) {
 *   console.log(`API Key: ${result1.config.value}`);
 *   console.log(`Origin: ${result1.config.origin.code}`);
 * }
 *
 * // Get a specific config value by code and level
 * const result2 = await getConfigurationByKey("enable_feature", byCodeAndLevel("website", "website"));
 *
 * // Get a specific config value by code
 * const result3 = await getConfigurationByKey("api_key", byCode("website"));
 * ```
 */
declare function getConfigurationByKey(configKey: string, selector: SelectorBy, options?: ConfigOptions): Promise<{
  scope: {
    id: string;
    code: string;
    level: string;
  };
  config: ConfigValue | null;
}>;
/**
 * Sets configuration values for a scope.
 *
 * This function updates configuration values for a specific scope. The provided values
 * are merged with existing configuration, and the origin is set to the current scope.
 * Configuration values are inherited from parent scopes unless explicitly overridden.
 *
 * @param request - Configuration set request containing the config values to set.
 * @param selector - Scope selector specifying how to identify the scope.
 * @param options - Optional library configuration options for cache timeout.
 * @returns Promise resolving to configuration response with updated scope and config values.
 *
 * @example
 * ```typescript
 * import { setConfiguration, byScopeId, byCodeAndLevel } from "@adobe/aio-commerce-lib-config";
 *
 * // Set configuration by scope ID
 * const result1 = await setConfiguration(
 *   {
 *     config: [
 *       { name: "api_key", value: "your-api-key-here" },
 *       { name: "enable_feature", value: true },
 *     ],
 *   },
 *   byScopeId("scope-123")
 * );
 *
 * // Set configuration by code and level
 * const result2 = await setConfiguration(
 *   {
 *     config: [
 *       { name: "timeout", value: 5000 },
 *       { name: "retry_count", value: 3 },
 *     ],
 *   },
 *   byCodeAndLevel("website", "website")
 * );
 *
 * console.log(result2.message); // "Configuration values updated successfully"
 * console.log(result2.scope); // Updated scope information
 * console.log(result2.config); // Array of updated config values
 * ```
 */
declare function setConfiguration(request: SetConfigurationRequest, selector: SelectorBy, options?: ConfigOptions): Promise<SetConfigurationResponse>;
/**
 * Sets the custom scope tree, replacing all existing custom scopes with the provided ones.
 *
 * Custom scopes allow you to define additional configuration scopes beyond the standard
 * Commerce scopes (global, website, store, store_view). This function replaces all
 * custom scopes, preserving system scopes (global and commerce).
 *
 * @param request - Custom scope tree request containing the scopes to set.
 * @param options - Optional library configuration options for cache timeout.
 * @returns Promise resolving to response with updated custom scopes.
 *
 * @example
 * ```typescript
 * import { setCustomScopeTree } from "@adobe/aio-commerce-lib-config";
 *
 * // Set custom scopes
 * const result = await setCustomScopeTree(
 *   {
 *     scopes: [
 *       {
 *         code: "region_us",
 *         label: "US Region",
 *         level: "custom",
 *         is_editable: true,
 *         is_final: false,
 *         children: [
 *           {
 *             code: "region_us_west",
 *             label: "US West",
 *             level: "custom",
 *             is_editable: true,
 *             is_final: true,
 *           },
 *         ],
 *       },
 *     ],
 *   }
 * );
 *
 * console.log(result.message); // "Custom scope tree updated successfully"
 * console.log(result.scopes); // Array of created/updated custom scopes
 * ```
 *
 * @example
 * ```typescript
 * import { setCustomScopeTree } from "@adobe/aio-commerce-lib-config";
 *
 * // Update existing custom scope (preserves ID if code and level match)
 * const result = await setCustomScopeTree(
 *   {
 *     scopes: [
 *       {
 *         id: "existing-scope-id", // Preserve existing ID
 *         code: "region_eu",
 *         label: "European Region",
 *         level: "custom",
 *         is_editable: true,
 *         is_final: false,
 *       },
 *     ],
 *   }
 * );
 * ```
 */
declare function setCustomScopeTree(request: SetCustomScopeTreeRequest, options?: OperationOptions): Promise<SetCustomScopeTreeResponse>;
//#endregion
//#region source/utils/encryption.d.ts
/**
 * Generates a new encryption key suitable for AIO_COMMERCE_CONFIG_ENCRYPTION_KEY.
 * @returns A hex string representing a 256-bit encryption key.
 */
declare function generateEncryptionKey(): string;
/**
 * Validates the encryption key.
 * @param key - The encryption key to validate.
 *
 * @throws {Error} If the encryption key is not a valid hex string or is not the correct length.
 */
declare function validateEncryptionKey(key: string): void;
//#endregion
export { type BusinessConfig, type BusinessConfigSchema, type BusinessConfigSchemaField, type BusinessConfigSchemaListOption, type BusinessConfigSchemaValue, CommerceScopeData, ConfigOptions, type ConfigOrigin, type ConfigValue, CustomScopeInput, CustomScopeOutput, GetCachedScopeTreeParams, GetConfigSchemaResponse, GetConfigurationByKeyResponse, GetConfigurationResponse, GetFreshScopeTreeParams, type GetScopeTreeResult, InitializeOptions, OperationOptions, SchemaBusinessConfig, type ScopeNode, type ScopeTree, type SelectorBy, type SelectorByCode, type SelectorByCodeAndLevel, type SelectorByScopeId, SetConfigurationRequest, SetConfigurationResponse, SetCustomScopeTreeRequest, SetCustomScopeTreeResponse, StoreGroup, StoreView, Website, byCode, byCodeAndLevel, byScopeId, generateEncryptionKey, getConfiguration, getConfigurationByKey, getScopeTree, initialize, setConfiguration, setCustomScopeTree, syncCommerceScopes, unsyncCommerceScopes, validateEncryptionKey };